## NOT CURRENTLY USED
databaseChangeLog:
  - changeset:
      id: createdb-personinfolocal-postgres
      author: bipref
#      runOnChange: false
      runInTransaction: false
      changes:
        - sql:
            sql: SELECT 'CREATE SCHEMA personinfolocal' WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'personinfolocal')\gexec
